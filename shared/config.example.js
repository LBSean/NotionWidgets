// Copy this file to config.js and fill in your worker URL.
// On Vercel, set the SYNC_URL environment variable instead — the build
// script will generate config.js automatically.
//
// config.js is git-ignored so your URL stays private.

var SYNC_URL = '';
