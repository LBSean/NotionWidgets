{
  "name": "notion-widgets",
  "version": "1.0.0",
  "description": "Personal Notion widgets hosted on GitHub Pages",
  "scripts": {
    "dev": "live-server --port=3000 --open=gallery.html",
    "preview": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
